mixin social(data)
	.social

		each val in data
			a.social__link(
				class= 'social__link_' + val.name
				href= val.link
				target='_blank'
				rel='nofollow'
			)
				i.fa(class= 'fa-' + val.name).social__icon