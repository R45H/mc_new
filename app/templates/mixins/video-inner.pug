mixin video-inner(link, type)
	.video-inner
		.video-inner__i
			video.video-inner__item(
				width='100%'
				height='100%'
			)
				source(
					src= link
					type= type || 'video/mp4; codecs="avc1.42E01E, mp4a.40.2"'
				)